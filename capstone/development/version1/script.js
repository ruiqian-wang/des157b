document.addEventListener('DOMContentLoaded', function() {

  // Interactive 3D model button
  document.querySelector('.interactive-btn').addEventListener('click', function() {
      // You can add specific interactions with your Spline model here
      alert('3D model in construction!');
  });


});

// Optional: Remove parallax effect since Spline handles its own 3D interactions
// window.addEventListener('scroll', function() {
//     // You can add custom scroll interactions here if needed
// });